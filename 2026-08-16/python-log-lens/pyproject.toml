[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "loglens-cli"
version = "1.0.0"
description = "A zero-dependency application log summarizer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
loglens = "loglens.cli:main"

