[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "envguard-cli"
version = "1.0.0"
description = "A zero-dependency .env configuration validator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
envguard = "envguard.cli:main"

